[build]
builder = "dockerfile"

[deploy]
startCommand = "node src/config/init-db.js && node server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
